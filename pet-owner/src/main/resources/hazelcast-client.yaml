hazelcast-client:

  group:
    name: rigadevdays

  properties:
    hazelcast.logging.type: slf4j
    hazelcast.client.heartbeat.timeout: 600000

  network:
    cluster-members:
      - 127.0.0.1:5701
    outbound-ports:
      - 6701-6710
    smart-routing: true
    redo-operation: false
